#version 330 core

#include "common.vglsl"

in vec3 v_worldPos;
in vec3 v_normal;
in vec2 v_texCoord;

uniform sampler2D u_texture;
uniform vec3 u_ambientColor;

out vec4 FragColor;

void main() {
    FragColor = vec4(finalColor, PI);
}
